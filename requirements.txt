# --- Web framework -----------------------------------------------------------
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0

# --- Config ------------------------------------------------------------------
python-dotenv>=1.0,<2.0

# --- LLM providers -----------------------------------------------------------
# Both are listed so you can switch providers with one env var. You only need a
# key for the one you actually use; the unused SDK is imported lazily and never
# touched at runtime if its key is absent.
anthropic>=0.40
openai>=1.30

# httpx is pulled in transitively by the SDKs; pinned here for reproducibility.
httpx>=0.27
